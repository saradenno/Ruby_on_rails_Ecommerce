<div class="grid md:grid-cols-2">
    <div class ="w-full flex flex-col items-center md:pt-8">
    <h1 class="font-miroles text-5xl text-black-100 mb-4 text-center"><%= @product.name %></h1>
    <div class="max-w-2xl">
        <%= image_tag(@product.images.first, class: " w-full h-96 object-cover rounded-lg") if @product.images.any? %>
        <div class="max-w-sm mt-4">
            <%= @product.description %>
        </div>
        </div>
    </div>

 
    <div data-controller="stripe-checkout" data-stripe-checkout-url-value="<%= product_buy_now_path(@product) %>"></div>
</div>
